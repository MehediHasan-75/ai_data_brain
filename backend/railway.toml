[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn expense_api.wsgi --workers 2 --timeout 120"
healthcheckPath = "/"
restartPolicyType = "on_failure"
